version: '3.8'

services:
  postgres:
    image: postgres:15-alpine
    container_name: pitch_db
    environment:
      POSTGRES_USER: pitch_admin
      POSTGRES_PASSWORD: pitch_password
      POSTGRES_DB: pitch_db
    ports:
      - "5432:5432"
    volumes:
      - postgres_data:/var/lib/postgresql/data
      - ./packages/db/schema.sql:/docker-entrypoint-initdb.d/schema.sql
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U pitch_admin -d pitch_db"]
      interval: 10s
      timeout: 5s
      retries: 5

volumes:
  postgres_data:
